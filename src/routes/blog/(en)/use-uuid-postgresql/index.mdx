---
title: Use UUID in PostgreSQL
description: Install and use UUID v4 and V7 in PostgreSQL
permalink: use-uuid-postgresql

tags:
  - postgresql

createdAt: 2024-05-22
---

import {BlogPost} from "~/components/blog-post";
import {PostHeader} from "~/components/post-header";

export const title = "Use UUID in PostgreSQL"
export const description = "Install and use UUID v4 and v7 in PostgreSQL"
export const tags  = ["postgresql"]

<PostHeader title={title} description={description} tags={tags} />
<BlogPost>
# Use uuid in PostgreSQL
</BlogPost>
